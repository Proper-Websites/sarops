.DS_Store
_render_tmp.html
_r.html
_sc*.html
_slidecheck.html

# Large local videos — host externally, not on GitHub (>100MB limit)
assets/video/*.mp4
sarops-videos-backup/

# Payment credentials — never commit
authnet-credentials.php

# Gallery source originals — site uses optimized copies in assets/in-action/
assets/In Action/

# Exception: events hero video, re-encoded to 7.6MB (under GitHub's 100MB limit)
!assets/video/training.mp4
